```
scss
/* Styles for card-option-ui component */
.card-option-container {
  display: flex;
  flex-direction: column;
  padding: 10px;
}
.button-primary {
  background-color: #007bff;
  color: white;
  border: none;
  padding: 8px 16px;
  cursor: pointer;
}
```

```
html
<div *ngIf="loading" class="loading-spinner">
  Loading...
</div>
<form (ngSubmit)="onSubmit()" #projectForm="ngForm" novalidate>
  <div class="form-group">
    <label for="projectName">Project Name</label>
    <input id="projectName" name="projectName" [(ngModel)]="project.name" required />
  </div>
  <button type="submit" [disabled]="!projectForm.valid">Add Project</button>
</form>
```